import {connect} from 'react-redux';
import ShareImage from './ShareImage';

const mapStateToProps = (state) => {
  return {

  };
};
function mapDispatchToProps(dispatch) {
  return { };
}

export default connect(mapStateToProps, mapDispatchToProps)(ShareImage);
