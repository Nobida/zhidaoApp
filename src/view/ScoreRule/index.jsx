import { connect } from 'react-redux';
import ScoreRule from './ScoreRule';

const mapStateToProps = (state) => {
  return {};
};
function mapDispatchToProps(dispatch) {
  return {};
}
export default connect(mapStateToProps, mapDispatchToProps)(ScoreRule);
